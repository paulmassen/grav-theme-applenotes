{% extends 'partials/base.html.twig' %}

{% block body %}
<div class="sidebar-layout dark:text-white h-dvh flex">
    {% include 'partials/sidebar.html.twig' %}
    
    <div class="flex-grow h-dvh content-area">
        <div class="w-full min-h-dvh p-3">
            <div class="h-full overflow-y-auto bg-background">
                <div class="px-2 py-8 text-center">
                    <h1 class="text-2xl font-bold mb-4">Error {{ page.header.http_response_code }}</h1>
                    <p class="text-muted-foreground">{{ page.content|raw }}</p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
